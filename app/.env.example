# 复制此文件为 .env 并填入真实值（云平台则在后台环境变量里配置，不要提交 .env）

# ---- 必填：云平台会自动注入 PORT，本地无需设置 ----
# PORT=8765

# ---- 可选：数据持久化到 GitHub ----
# 作用：上传新数据后自动把 CSV 源数据备份到仓库；
#       云平台容器重启丢失数据时，自动从仓库拉取恢复。
# 不配置也能正常运行，只是重启后数据不保留。
# GITHUB_TOKEN=github_pat_xxxxxxxxxxxx
# GITHUB_REPO=jiaxingbillyli-rgb/moto-dashboard
# GITHUB_BRANCH=main

# ---- 可选：监听地址（默认：有 PORT 时 0.0.0.0，否则 127.0.0.1）----
# HOST=0.0.0.0
