# 看板云端部署指南（动态版）

把看板从"只能看的静态页"升级为**任何人打开网址都能上传月报、数据即时更新**的动态版。

---

## 一、现在是什么状态

| 项目 | 说明 |
|--|--|
| 静态版（已有） | `https://jiaxingbillyli-rgb.github.io/moto-dashboard/` —— 只能看，我在本机更新后推送 |
| 动态版（本次目标） | 部署到云平台，任何设备打开网址都能上传 Excel，数据立刻生效，所有人可见 |

**好消息**：前端上传功能早就内置了（页面右上角「📤 上传更新文件」按钮），只是静态托管没有后端接不住。把后端跑起来即可，前端不用改。

本次已完成的代码改造：
- `server.py`：端口改从环境变量读（云平台自动注入），带 `PORT` 时自动监听 `0.0.0.0`
- 新增 **GitHub 数据持久化**：上传后自动把 CSV 源数据备份到仓库；容器重启丢失数据时自动拉取恢复
- 新增部署文件：`requirements.txt`、`Procfile`、`runtime.txt`、`.env.example`、`.gitignore`

实测：上传一个月报 → 解析 + 聚合耗时约 **7 秒**。

---

## 二、部署到哪（三选一）

| 方案 | 费用 | 国内访问 | 缺点 | 适合 |
|--|--|--|--|--|
| **Render** | 免费 | 较慢 | 15 分钟无访问会休眠，冷启动 30~60 秒 | 先免费试效果 |
| **Railway** | 有免费额度 | 较慢 | 额度用完后需付费 | 备选 |
| **国内云服务器**（腾讯云/阿里云轻量） | 约 24~60 元/月 | **快且稳** | 需付费 | 正式长期使用 |

> 建议：先用 Render 免费版验证流程跑通，确认好用后再决定是否迁到国内云。

---

## 三、Render 部署步骤（推荐先试）

### 第 1 步：准备代码仓库

云平台从 GitHub 仓库拉代码，需要一个**结构完整**的仓库，必须包含：

```
server.py
requirements.txt
Procfile
runtime.txt
scripts/          （解析脚本）
output/           （dashboard.html / dashboard.js / echarts.min.js + CSV 源数据）
```

> 注意：现有 `moto-dashboard` 仓库是给 GitHub Pages 用的静态站（文件在根目录），
> 结构不同，**不能直接拿来部署后端**。需要单独建一个仓库（例如 `moto-dashboard-server`）。

### 第 2 步：在 Render 创建服务

1. 打开 https://render.com ，用 **GitHub 账号登录**
2. 点 **New +** → **Web Service**
3. 选择第 1 步准备的仓库，点 Connect
4. 配置：

| 字段 | 填什么 |
|--|--|
| Name | `moto-dashboard`（随意）|
| Runtime | Python 3 |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `python server.py` |
| Instance Type | **Free** |

5. 点 **Create Web Service**

首次构建要装 pandas/numpy，大约需要 **3~5 分钟**。

### 第 3 步：配置环境变量（可选但强烈建议）

在服务页面 → **Environment** → 添加：

| 变量名 | 值 | 作用 |
|--|--|
| `GITHUB_TOKEN` | 你的 GitHub token | 上传后把 CSV 备份到仓库 |
| `GITHUB_REPO` | `jiaxingbillyli-rgb/moto-dashboard` | 备份目标仓库 |
| `GITHUB_BRANCH` | `main` | 分支（可省略，默认 main）|

> **为什么要配**：Render 免费版容器重启会清空磁盘。配了之后，重启时会自动从
> GitHub 把 CSV 拉回来重新生成数据，不会丢。不配也能跑，只是重启后数据没了。

### 第 4 步：验证

部署完成后 Render 会给你一个网址，例如 `https://moto-dashboard.onrender.com`

1. 打开该网址 → 应看到看板（和现在一样）
2. 点右上角「📤 上传更新文件」→ 选一个月报 Excel
3. 等待约 10 秒 → 提示上传成功 → 刷新页面，数据已更新
4. 用手机打开同一网址 → 看到的是同样最新的数据 ✅

---

## 四、国内云服务器部署（正式使用）

以腾讯云轻量应用服务器为例（阿里云同理）：

```bash
# 1. 装 Python 3.11 和 git 后，拉取代码
git clone https://github.com/<你的账号>/moto-dashboard-server.git
cd moto-dashboard-server

# 2. 装依赖
pip install -r requirements.txt

# 3. 配环境变量（数据持久化用，可选）
export GITHUB_TOKEN=你的token
export GITHUB_REPO=jiaxingbillyli-rgb/moto-dashboard

# 4. 启动（后台常驻）
nohup python server.py > server.log 2>&1 &
```

服务器需放行端口（腾讯云在控制台「防火墙」里添加规则，TCP 8765）。
访问 `http://<服务器公网IP>:8765`。

建议再用 nginx 做反向代理 + 配域名，此处不展开。

---

## 五、两个网址的关系（重要）

| 网址 | 类型 | 数据 | 能否上传 |
|--|--|--|--|
| `jiaxingbillyli-rgb.github.io/moto-dashboard/` | 静态镜像 | 我推送时才更新 | ❌ |
| 云平台网址（Render/云服务器） | **动态版** | 上传即更新 | ✅ |

建议后续以**云平台网址为主入口**，分享给同事/手机收藏都用这个。
Pages 那个可以保留作为备份镜像。

---

## 六、常见问题

**Q：免费版休眠怎么办？**
A：Render 免费版 15 分钟无访问会休眠，下次访问要等 30~60 秒冷启动。
介意的话用国内云服务器（约 24 元/月起），或给 Render 升级付费版。

**Q：数据会不会丢？**
A：配了 `GITHUB_TOKEN` 就不会——CSV 源数据会备份到 GitHub，容器重启自动恢复。

**Q：上传后多久生效？**
A：约 7~10 秒（解析 + 重新聚合）。上传期间页面会显示进度。

**Q：多人同时上传会冲突吗？**
A：目前是单进程串行处理，同时上传会排队，不会损坏数据。
